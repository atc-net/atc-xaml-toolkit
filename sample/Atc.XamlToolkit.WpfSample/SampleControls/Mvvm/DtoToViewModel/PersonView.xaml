<UserControl
    x:Class="Atc.XamlToolkit.WpfSample.SampleControls.Mvvm.DtoToViewModel.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:person="clr-namespace:Atc.XamlToolkit.WpfSample.SampleControls.Mvvm.DtoToViewModel"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <person:PersonViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0">First name</TextBlock>
        <TextBox
            Grid.Row="0"
            Grid.Column="2"
            Text="{Binding Path=FirstName}" />

        <TextBlock Grid.Row="1" Grid.Column="0">Last name</TextBlock>
        <TextBox
            Grid.Row="1"
            Grid.Column="2"
            Text="{Binding Path=LastName}" />

        <TextBlock Grid.Row="2" Grid.Column="0">Age</TextBlock>
        <TextBox
            Grid.Row="2"
            Grid.Column="2"
            Text="{Binding Path=Age}" />

    </Grid>

</UserControl>