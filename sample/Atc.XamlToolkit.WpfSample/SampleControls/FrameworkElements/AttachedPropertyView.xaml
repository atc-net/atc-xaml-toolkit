<UserControl
    x:Class="Atc.XamlToolkit.WpfSample.SampleControls.FrameworkElements.AttachedPropertyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attachedPropertyComponents="clr-namespace:Atc.XamlToolkit.WpfSample.SampleControls.FrameworkElements.AttachedPropertyComponents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Text="AttachedProperty Source Generator Examples" />

            <Separator Margin="0,10" />

            <!--  Example 1: Class-Level Attribute - Watermark Behavior  -->
            <GroupBox Margin="0,10" Header="Example 1: Class-Level Attribute - Watermark Behavior">
                <StackPanel Margin="10">
                    <TextBlock
                        Margin="0,0,0,10"
                        Text="The WatermarkBehavior is defined using [AttachedProperty&lt;string&gt;(&quot;Watermark&quot;)] at the class level."
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,0,0,10"
                        FontStyle="Italic"
                        Foreground="CadetBlue"
                        Text="Code: [AttachedProperty&lt;string&gt;(&quot;Watermark&quot;, DefaultValue = &quot;&quot;, PropertyChangedCallback = nameof(OnWatermarkChanged))]"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,0,0,10"
                        Text="Enter text in the TextBoxes below. The watermark text will disappear when focused and reappear if empty:"
                        TextWrapping="Wrap" />

                    <TextBox
                        Width="300"
                        Margin="0,5"
                        HorizontalAlignment="Left"
                        attachedPropertyComponents:WatermarkBehavior.Watermark="Enter your name..." />

                    <TextBox
                        Width="300"
                        Margin="0,5"
                        HorizontalAlignment="Left"
                        attachedPropertyComponents:WatermarkBehavior.Watermark="Enter your email address..." />

                    <TextBox
                        Width="300"
                        Margin="0,5"
                        HorizontalAlignment="Left"
                        attachedPropertyComponents:WatermarkBehavior.Watermark="Enter your phone number..." />

                    <TextBlock
                        Margin="0,10,0,0"
                        FontSize="12"
                        Foreground="Gray"
                        Text="Generated code: GetWatermark(), SetWatermark(), WatermarkProperty"
                        TextWrapping="Wrap" />
                </StackPanel>
            </GroupBox>

            <!--  Example 2: Field-Level Attribute - Highlight Behavior  -->
            <GroupBox Margin="0,10" Header="Example 2: Field-Level Attribute - Highlight Behavior">
                <StackPanel Margin="10">
                    <TextBlock
                        Margin="0,0,0,10"
                        Text="The HighlightBehavior is defined using [AttachedProperty] on private fields, which infers property names and types."
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,0,0,10"
                        FontStyle="Italic"
                        Foreground="CadetBlue"
                        Text="Code: [AttachedProperty(PropertyChangedCallback = nameof(OnIsHighlightEnabledChanged))] private static bool isHighlightEnabled;"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,0,0,10"
                        Text="Hover over the buttons and borders below to see the highlight effect (buttons dim, borders change color):"
                        TextWrapping="Wrap" />

                    <!--  Example 2a: Basic Highlight  -->
                    <TextBlock
                        Margin="0,10,0,5"
                        FontWeight="SemiBold"
                        Text="Basic Highlight:" />

                    <StackPanel Margin="0,5" Orientation="Horizontal">
                        <Button
                            Margin="0,0,10,0"
                            Padding="15,8"
                            attachedPropertyComponents:HighlightBehavior.IsHighlightEnabled="True"
                            Content="Hover Me" />

                        <Border
                            Width="120"
                            Height="40"
                            Margin="0,0,10,0"
                            attachedPropertyComponents:HighlightBehavior.IsHighlightEnabled="True"
                            Background="LightGreen">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Text="Hover Border" />
                        </Border>
                    </StackPanel>

                    <!--  Example 2b: Disabled Highlight  -->
                    <TextBlock
                        Margin="0,15,0,5"
                        FontWeight="SemiBold"
                        Text="Highlight Disabled:" />

                    <Button
                        Margin="0,5"
                        Padding="15,8"
                        attachedPropertyComponents:HighlightBehavior.IsHighlightEnabled="False"
                        Content="No Highlight" />

                    <TextBlock
                        Margin="0,10,0,0"
                        FontSize="12"
                        Foreground="Gray"
                        Text="Generated code: GetIsHighlightEnabled(), SetIsHighlightEnabled(), IsHighlightEnabledProperty"
                        TextWrapping="Wrap" />
                </StackPanel>
            </GroupBox>

            <!--  Comparison Section  -->
            <GroupBox Margin="0,10" Header="Comparison: Class vs Field Attribute">
                <StackPanel Margin="10">
                    <TextBlock
                        Margin="0,0,0,10"
                        FontWeight="SemiBold"
                        Text="Class-Level Attribute Approach:" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ Explicit property name and type declaration"
                        TextWrapping="Wrap" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ Good for single attached properties"
                        TextWrapping="Wrap" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ More visible at class level"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,15,0,10"
                        FontWeight="SemiBold"
                        Text="Field-Level Attribute Approach:" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ Less boilerplate - name and type inferred"
                        TextWrapping="Wrap" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ Great for multiple related properties"
                        TextWrapping="Wrap" />
                    <TextBlock
                        Margin="10,0,0,5"
                        Text="✓ More concise and DRY (Don't Repeat Yourself)"
                        TextWrapping="Wrap" />

                    <TextBlock
                        Margin="0,15,0,0"
                        FontStyle="Italic"
                        Foreground="DarkGreen"
                        Text="Both approaches generate the same dependency property infrastructure!"
                        TextWrapping="Wrap" />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
