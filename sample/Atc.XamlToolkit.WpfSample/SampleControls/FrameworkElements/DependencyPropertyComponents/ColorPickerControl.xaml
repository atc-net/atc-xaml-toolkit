<UserControl
    x:Class="Atc.XamlToolkit.WpfSample.SampleControls.FrameworkElements.DependencyPropertyComponents.ColorPickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="150"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Border
        Padding="10"
        Background="Chocolate"
        BorderBrush="Gray"
        BorderThickness="1"
        CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Red Slider  -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="Red:" />
                <Slider
                    x:Name="RedSlider"
                    Grid.Column="1"
                    Maximum="255"
                    Minimum="0"
                    Value="{Binding Red, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
                <TextBlock
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Binding Value, ElementName=RedSlider, StringFormat='{}{0:F0}'}" />
            </Grid>

            <!--  Green Slider  -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="Green:" />
                <Slider
                    x:Name="GreenSlider"
                    Grid.Column="1"
                    Maximum="255"
                    Minimum="0"
                    Value="{Binding Green, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
                <TextBlock
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Binding Value, ElementName=GreenSlider, StringFormat='{}{0:F0}'}" />
            </Grid>

            <!--  Blue Slider  -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Text="Blue:" />
                <Slider
                    x:Name="BlueSlider"
                    Grid.Column="1"
                    Maximum="255"
                    Minimum="0"
                    Value="{Binding Blue, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
                <TextBlock
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Binding Value, ElementName=BlueSlider, StringFormat='{}{0:F0}'}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
