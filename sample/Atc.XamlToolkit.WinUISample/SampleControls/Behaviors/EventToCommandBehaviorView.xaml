<UserControl
    x:Class="Atc.XamlToolkit.WinUISample.SampleControls.Behaviors.EventToCommandBehaviorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Atc.XamlToolkit.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Atc.XamlToolkit.WinUISample.SampleControls.Behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <local:EventToCommandBehaviorViewModel />
    </UserControl.DataContext>

    <ScrollViewer>
        <StackPanel Margin="20" Spacing="10">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Text="EventToCommandBehavior Sample" />

            <MenuFlyoutSeparator />

            <!--  Example 1: Button Click Event  -->
            <Border
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5">
                <StackPanel Spacing="10">
                    <TextBlock FontWeight="SemiBold" Text="Example 1: Button Click Event" />

                    <TextBlock Text="Click the button to execute a command via EventToCommandBehavior:" TextWrapping="Wrap" />

                    <Button
                        Width="200"
                        Padding="10,5"
                        HorizontalAlignment="Left"
                        Content="Click Me">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:EventToCommandBehavior Command="{x:Bind ViewModel.ButtonClickCommand}" EventName="Click" />
                        </interactivity:Interaction.Behaviors>
                    </Button>

                    <TextBlock>
                        <Run Text="Button clicked: " />
                        <Run FontWeight="Bold" Text="{x:Bind ViewModel.ClickCount, Mode=OneWay}" />
                        <Run Text=" times" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!--  Example 2: ListBox SelectionChanged with EventArgs  -->
            <Border
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5">
                <StackPanel Spacing="10">
                    <TextBlock FontWeight="SemiBold" Text="Example 2: ListBox SelectionChanged with EventArgs" />

                    <TextBlock Text="Select items to see the selection changed event passed to the command:" TextWrapping="Wrap" />

                    <ListBox
                        Width="300"
                        Height="120"
                        HorizontalAlignment="Left"
                        ItemsSource="{x:Bind ViewModel.Items}"
                        SelectionMode="Single">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:EventToCommandBehavior
                                Command="{x:Bind ViewModel.SelectionChangedCommand}"
                                EventName="SelectionChanged"
                                PassEventArgsToCommand="True" />
                        </interactivity:Interaction.Behaviors>
                    </ListBox>

                    <TextBlock TextWrapping="Wrap">
                        <Run Text="Last selection: " />
                        <Run FontWeight="Bold" Text="{x:Bind ViewModel.LastSelection, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!--  Example 3: TextBox TextChanged with CommandParameter  -->
            <Border
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5">
                <StackPanel Spacing="10">
                    <TextBlock FontWeight="SemiBold" Text="Example 3: TextBox TextChanged with CommandParameter" />

                    <TextBlock Text="Type in the textbox to trigger the command with a custom parameter:" TextWrapping="Wrap" />

                    <TextBox Width="300" HorizontalAlignment="Left">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:EventToCommandBehavior
                                Command="{x:Bind ViewModel.TextChangedCommand}"
                                CommandParameter="From TextBox"
                                EventName="TextChanged" />
                        </interactivity:Interaction.Behaviors>
                    </TextBox>

                    <TextBlock TextWrapping="Wrap">
                        <Run Text="Text changed triggered: " />
                        <Run FontWeight="Bold" Text="{x:Bind ViewModel.TextChangeCount, Mode=OneWay}" />
                        <Run Text=" times" />
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="Parameter received: " />
                        <Run FontWeight="Bold" Text="{x:Bind ViewModel.LastParameter, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!--  Example 4: PointerEntered and PointerExited Events  -->
            <Border
                Padding="10"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5">
                <StackPanel Spacing="10">
                    <TextBlock FontWeight="SemiBold" Text="Example 4: PointerEntered and PointerExited Events" />

                    <TextBlock Text="Hover over the border to trigger pointer events:" TextWrapping="Wrap" />

                    <Border
                        Width="300"
                        Height="100"
                        HorizontalAlignment="Left"
                        Background="{x:Bind ViewModel.BorderBackground, Mode=OneWay}"
                        BorderBrush="Gray"
                        BorderThickness="2"
                        CornerRadius="5">
                        <interactivity:Interaction.Behaviors>
                            <behaviors:EventToCommandBehavior Command="{x:Bind ViewModel.MouseEnterCommand}" EventName="PointerEntered" />
                            <behaviors:EventToCommandBehavior Command="{x:Bind ViewModel.MouseLeaveCommand}" EventName="PointerExited" />
                        </interactivity:Interaction.Behaviors>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="Hover over me!" />
                    </Border>

                    <TextBlock>
                        <Run Text="Pointer state: " />
                        <Run FontWeight="Bold" Text="{x:Bind ViewModel.MouseState, Mode=OneWay}" />
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
