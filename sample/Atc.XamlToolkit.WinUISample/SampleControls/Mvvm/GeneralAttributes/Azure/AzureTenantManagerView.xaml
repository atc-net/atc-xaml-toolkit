<UserControl
    x:Class="Atc.XamlToolkit.WinUISample.SampleControls.Mvvm.GeneralAttributes.Azure.AzureTenantManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:azure="using:Atc.XamlToolkit.WinUISample.SampleControls.Mvvm.GeneralAttributes.Azure"
    mc:Ignorable="d">

    <Grid ColumnSpacing="10" RowSpacing="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center">
            Is Signed In
        </TextBlock>
        <CheckBox
            Grid.Row="0"
            Grid.Column="1"
            IsChecked="{x:Bind ViewModel.IsSignedIn, Mode=OneWay}"
            IsEnabled="False" />

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            VerticalAlignment="Center">
            Signed In User
        </TextBlock>
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Text="{x:Bind ViewModel.SignedInUser, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            VerticalAlignment="Center">
            Selected Environment Key
        </TextBlock>
        <TextBox
            Grid.Row="2"
            Grid.Column="1"
            Text="{x:Bind ViewModel.SelectedEnvironmentKey, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="3"
            Grid.Column="0"
            VerticalAlignment="Center">
            Default Azure Account
        </TextBlock>
        <TextBox
            Grid.Row="3"
            Grid.Column="1"
            Text="{x:Bind ViewModel.DefaultAzureAccount, Mode=TwoWay}" />

        <TextBlock
            Grid.Row="4"
            Grid.Column="0"
            VerticalAlignment="Center">
            Current Environment Type
        </TextBlock>
        <TextBlock
            Grid.Row="4"
            Grid.Column="1"
            VerticalAlignment="Center"
            Text="{x:Bind ViewModel.CurrentEnvironmentType, Mode=OneWay}" />

        <Button
            Grid.Row="6"
            Grid.Column="0"
            Command="{x:Bind ViewModel.LoginCommand}"
            Content="Login" />
        <Button
            Grid.Row="6"
            Grid.Column="1"
            Command="{x:Bind ViewModel.LogoutCommand}"
            Content="Logout" />

        <TextBlock
            Grid.Row="7"
            Grid.Column="0"
            VerticalAlignment="Center">
            Is Busy
        </TextBlock>
        <CheckBox
            Grid.Row="7"
            Grid.Column="1"
            IsChecked="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
            IsEnabled="False" />

    </Grid>

</UserControl>
