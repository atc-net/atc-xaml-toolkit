<UserControl
    x:Class="Atc.XamlToolkit.WinUISample.SampleControls.Mvvm.DtoToViewModel.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dtoToViewModel="using:Atc.XamlToolkit.WinUISample.SampleControls.Mvvm.DtoToViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="10">
        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Text="PersonView - Dto To ViewModel Demo" />

        <TextBlock Text="This demonstrates the ObservableDtoViewModel source generator." TextWrapping="Wrap" />

        <Grid ColumnSpacing="10" RowSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="First Name:" />
            <TextBox
                Grid.Row="0"
                Grid.Column="1"
                Text="{x:Bind ViewModel.FirstName, Mode=TwoWay}" />

            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="Last Name:" />
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Text="{x:Bind ViewModel.LastName, Mode=TwoWay}" />

            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="Age:" />
            <NumberBox
                Grid.Row="2"
                Grid.Column="1"
                SpinButtonPlacementMode="Inline"
                Value="{x:Bind ViewModel.Age, Mode=TwoWay}" />
        </Grid>

        <TextBlock FontWeight="SemiBold" Text="Current Values:" />
        <TextBlock Text="{x:Bind ViewModel.FirstName, Mode=OneWay}" />
        <TextBlock Text="{x:Bind ViewModel.LastName, Mode=OneWay}" />
        <TextBlock Text="{x:Bind ViewModel.Age, Mode=OneWay}" />
    </StackPanel>

</UserControl>
